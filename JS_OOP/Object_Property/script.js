'use strict';
var cat = {
  name: 'myCat',
  color: 'white'
}

object.getOwnProperty(cat, 'name');
// 4 configurations: writable, enumerable, configurable, and value

// Object.freeze will prevent the object from being changed

// Configurable is to change enumerable & value
